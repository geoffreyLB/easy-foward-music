import {
  SET_ACCESS_TOKEN,
  SET_TOKEN_TYPE,
  SET_EXPIRESIN,
  SET_PROFIL_DATA,
  SET_PLAYLIST_DATA,
} from '@actions/actions';

const deezerReducer = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default deezerReducer;
